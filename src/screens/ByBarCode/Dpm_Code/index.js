import {StyleSheet, View} from 'react-native';
import React from 'react';
import {Scan, Text} from '../../../components';

const DpmCode = ({
  navigation,
  route,
}) => {
  return (
    <View>
      <Scan />
    </View>
  );
};

export default DpmCode;

const styles = StyleSheet.create({});
