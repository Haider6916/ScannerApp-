import React from 'react';
import {StyleSheet, View} from 'react-native';
import { Scan } from '../../../components';

const D_Retail= ({
  navigation,
  route,
}) => {
  return (
    <View>
      <Scan />
    </View>
  );
};

export default D_Retail;

const styles = StyleSheet.create({});
