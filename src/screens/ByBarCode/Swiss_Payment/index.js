import {StyleSheet, View} from 'react-native';
import {Scan, Text} from '../../../components';
import React from 'react';

const SwissPayment = ({navigation, route}) => {
  return (
    <View>
      <Scan />
    </View>
  );
};

export default SwissPayment;

const styles = StyleSheet.create({});
