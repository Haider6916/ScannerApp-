import {StyleSheet, View} from 'react-native';
import React from 'react';
import {Text} from '../../../components';

const MultipleCode = ({navigation, route}) => {
  return (
    <View>
      <Text
        style={{
          fontSize: 16,
          color: 'blue',
          textAlign: 'center',
        }}>
        MultipleCode
      </Text>
    </View>
  );
};

export default MultipleCode;

const styles = StyleSheet.create({});
